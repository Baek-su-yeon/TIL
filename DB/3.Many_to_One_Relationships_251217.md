## 모델 관계

|종류|설명|예|
|:---|:---|:---|
|1:1(One to One)|한 테이블의 레코드는 다른 테이블의 한 레코드와 연결|한 사람 당 하나의 주민번호|
|N:1(Many to One)|여러 개의 레코드가 하나의 레코드와 연결|여러 교육생을 한 강사가 교육|
|N:M(Many to Many)|여러 레코드가 여러 레코드와 상호 연결|여러 학생이 여러 과목 수강|

### Foreign Key
- N:1 관계에서는 N을 담당하는 테이블에 위치, 참조하는 데이터를 대표하는 Primary Key(PK)를 저장

    > 기본형
    >> **ForeginKey(to, on_delete)**

    - **to 속성**: 참조하는 모델 클래스 이름(즉, N:1에서 1의 정보)
    - **on_delete 속성**: 외래 키가 참조하는 객체가 사라졌을 때, 외래 키를 가진 객체를 **어떻게 처리할 지를 정의**

    |속성명|설명|
    |:---|:---|
    |CASCADE|참조된 객체(부모 객체)가 삭제될 때 이를 참조하는 모든 객체도 삭제|
    |PROTECT|삭제하려는 부모 객체에 자식 객체가 존재한다면 해당 부모 객체 삭제 불가|
    |SET_NULL|부모 객체가 삭제되면, 해당 필드에 값을 NULL로 저장(🚨필드 설정 null=True 필요)|

---

## 모델 참조
- 직접 대상 정보를 저장하고 필요할 때 활용하는 것

    ### 역참조
    - 누가 나를 참조하는지 거꾸로 조회하는 것 (즉, 내 번호를 저장한 사람들이 누구인지 찾는 것)

        >**Model_Instance.Related_manager.QuerySet_API**

        |이름|설명|
        |:---|:---|
        |Model Instance|models.py에 정의된 모델 클래스로 **참조 가능한 필드가 없는** 모델 클래스의 인스턴스를 사용|
        |Related Manager|N:1 혹은 N:M 관계에서 역참조 시에 사용하는 매니저|
        |QuerySet API|데이터를 가져오기 위한 쿼리 결과 집합을 만드는 인터페이스 (예: all() 등)|

        ✳️ **Related Manager 이름 규칙**
        - 모델 클래스명 + _set이 기본 값이고 Django에서 자동 생성
        - 댓글과 게시글 관계예서 참조와 역참조
            > 참조: comment.article
            
            > 역참조: article.comment_set.all()
