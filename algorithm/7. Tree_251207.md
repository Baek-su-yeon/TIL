## Tree란?
- 비선형 구조로 원소들 간에 1:n 관계를 가지는 자료구조
- 원소들 간에 계층 관계를 가져 계층형 자료구조라고도 함

|용어|설명|
|:---|:---|
|노드(node)|트리의 원소|
|간선(edge)|노드를 연결하는 선|
|서브 트리|부모 노드와 연결된 간선을 끊었을 때 생성되는 트리|
|노드의 차수|노드에 연결된 자식 노드의 수|
|노드의 높이|루트에서 노드에 이르는 간선의 수|
|트리의 차수|트리에 있는 노드의 차수 중 가장 큰 값|
|트리의 높이|트리에 있는 노드의 높이 중 가장 큰 값|
|루트 노드|트리의 시작|
|형제 노드|같은 부모 노드의 자식 노드들|
|조상 노드|간선을 따라 루트 노드까지 이르는 경로에 있는 모든 노드들|
|자손 노드|서브 트리에 있는 하위 레벨의 노드들|
|단말 노드(leaf node)|차수가 0인 노드, 자식 노드가 없는 노드|

## 이진트리란?
- 모든 노드들이 2개의 서브트리를 갖는 특별한 형태의 트리
- **각 노드가 자식 노드를 최대한 2개**까지만 가질 수 있는 트리

    ### 특성
    1. 레벨 i에서의 노드 최대 개수 = $2^i$(🚨처음 레벨 값이 1일 경우는 $2^i - 1$)
    2. 높이가 h일 때 노드의 최소 개수 = $h + 1$, 최대 개수 = $2^{h+1} - 1$

    ### 종류
    1. 포화 이진 트리 (Full Binary Tree)
    모든 레벨에 노드가 포화 상태로 차 있는 이진 트리
    ✅ 즉, 높이 = h이면 노드 수 = $2^{h+1} - 1$

    2. 완전 이진 트리 (Complete Binaray Tree)
    노드 수가 n개 일 때 노드 번호 1번부터 n번까지만 차 있는 이진 트리 (단, $2^h <= n <= 2^{h+1} - 1$)

    3. 편향 이진 트리 (Skewed Binary Tree)
    높이가 h일 때 **최소 노드** 개수를 가지고 **한쪽 방향**의 자식만 가진 이진 트리

## 이진 트리의 표현
- 
    ### 배열(인덱스)
    포화 이진 트리, 완전 이진 트리에 적합

    |조건|노드|번호|
    |:---|:---|:---|
    |노드 번호 = i|부모 노드|&i / 2&|
    ||왼쪽 자식 노드|$2 * i$|
    ||오른쪽 자식 노드|$2 * i + 1$|
    |레벨 n|시작 노드|$2^n$|
    |높이 h|배열의 크기|$2^{h+1} - 1$|

    - 응용 1. 부모 노드 번호를 인덱스로 사용해서 자식 노드 번호를 저장
    ```python
    # python
    """
    입력 예시
    4
    1 2 1 3 3 4 3 5
    """

    e = int(input())
    n = 5
    tree = list(map(int, input().split()))

    left_child = [0] * (n + 1)
    right_child = [0] * (n + 1)

    for i in range(e):
        p = tree[i * 2]
        c = tree[i * 2 + 1]

        if left_child[p] == 0: # 노드에 왼쪽 자식이 없다.
            left_child[p] = c
        else: # 있으면 오른쪽 채우기
            right_child[p] = c
    ```

    - 응용 2. 자식 노드 번호를 인덱스로 부모 노드 번호를 저장하는 방법 (루트 찾기, 조상 찾기에 많이 활용)
    ```python
    # python
    """
    입력 예시
    4
    1 2 1 3 3 4 3 5
    """
    
    e = int(input())
    n = 5
    tree = list(map(int, input().split()))

    parent = [0] * (n + 1)

    for i in range(e):
        p = tree[i * 2]
        c = tree[i * 2 + 1]

        parent[c] = p # 자식 c는 부모 p와 연결되어 있다.

    # 5번 노드의 조상 노드 모두 찾기
    child = 5

    ancestor = []

    while parent[child] != 0: # 0이라는 것은 부모가 없음 = 루트 노드
        child = parent[child]
        ancestor.append(child)
    ```

    ### 연결 리스트
    ```python
    # python

    class Node:
        def __init__(self, value):
            self.left = None
            self.right = None
            self.value = None
    
    A = Node("A")
    B = Node("B")
    C = Node("C")

    A.left = B
    A.right = C
    ```

## 순회, traversal
- 트리의 각 노드를 중복되지 않게 전부 방문하는 것

    ### 전위순회, preorder traversal
    부모 노드 ▶️ 왼쪽 자식 노드 ▶️ 오른쪽 자식 노드(VLR)
    ```python
    # python
    def preorder_traversal(node):
        if node: # T is not None
            # TODO: 현재 노드 n에서 해야할 것
            preorder_traversal(node.left)
            preorder_traversal(node.right)
    ```

    ### 중위순회, inorder traversal
    왼쪽 자식 노드 ▶️ 부모 노드 ▶️ 오른쪽 자식 노드(LVR)
    ```python
    # python
    def inorder_traversal(node):
        if node:
            inorder_traversal(node.left)
            # TODO
            inorder_traversal(node.right)
    ```

    ### 후위순회, postorder traversal
    왼쪽 자식 노드 ▶️ 오른쪽 자식 노드 ▶️ 부모 노드(LRV)
    ```python
    # python
    def postorder_traversal(node):
        if node:
            postorder_traversal(node.left)
            postorder_traversal(node.right)
            # TODO
    ```