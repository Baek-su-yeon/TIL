## Form이란?
- 사용자 **입력 데이터를 수집**하고, 처리 및 유효성 검사를 수행하기 위한 도구
- 유효성 검사를 단순화하고 자동화 할 수 있는 기능 제공
- 사용자가 잘못 입력한 데이터는 자동으로 오류 처리 ➡ 안전성 증가
    
    ```python
    # 기본구조 및 활용
    # app_name/forms.py

    from django import forms

    class FormName(forms.Form):
        Field_name = forms.Field_type()
        Field_name2 = forms.Field_type(widget=forms.Textarea)
        # widget: HTML 'input' elelment의 표현을 담당
        # form의 각 필드가 HTML에서 어떻게 렌더링 될지 결정

    # app_name/views.py
    from .forms import FormName

    def function(request):
        form = FormName()
        context = {
            'form':form,
        }
        return render(request,'app_name/your_page.html', context)
    # 이후 html에서 form에 정의된 내용대로 입력창 생성
    ```

---

## ModelForm
- Model과 연결된 Form을 자동으로 생성해주는 기능 제공
- Form 클래스와 Model 클래스를 결합한 형태로, **모델 필드를 기반**으로 입력 폼 자동 생성
    ```python
    # 기본 구조 및 활용
    # app_name/forms.py
    form django import forms
    from .models import ModelName

    class ModelNameForm(forms.ModelForm):
        class Meta:
            model = ModelName # form의 동작 방식을 제어
            fields = '__all__' # model의 모든 필드 다 가져옴
            # fields = ('Field_name', ) # Field_name만 가져옴, ',' 필수
            # exclude = ('Field_name', ) # Field_name만 제외하고 가져옴

    # app_name/views.py
    from .forms import ModelNameForm

    def fuction(request):
        form = ModelNameForm(request.POST)

        """
        # update, edit 등 원래 객체의 정보가 필요할 때
        object_name = ModelName.object.get(pk=pk)
        form = ModelNameForm(request.POST, instance=object_name) # instance로 객체를 넘겨줘야함
        """

        if form.is_valid(): # is_valid(): 유효성 검사 실행
            form.save() # 
            return redirect('app_name:your_nametag')

            """
            retunr에 pk 등 객체의 정보를 넘겨야 한다면,
            object_name = form.save()
            return redirect('app_name:your_nametag', object_name.pk)
            """

        # 유효성 검사 결과 = False, 에러 메세지 포함 아래 코드 실행
        context={
            'form':form,
        }
        return render(request,'app_name/your.html', context)
    ```
    ### Meta class
    - ModelForm의 정보를 작성하는 곳
    - ModelForm 내부에서 어떤 모델과 연결할지, 어떤 필드를 사용할지 등을 정의하는 설정 공간

    ### save()
    - 데이터베이스 객체를 만들고 저장하는 ModleForm의 인스턴스 메서드
    - instance 인자를 통해 새 객체 생성과 기존 객체 수정도 구분

---

## 참고
### 📌 Form vs ModelForm

|Form|ModelForm|
|:---|:---|
|사용자 입력 데이터를 DB에 저장하지 않을 때|사용자 입력 데이터를 DB에 저장해야할 때|
|검색, 로그인|게시글 작성, 회원가입|